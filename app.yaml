name: langchain-chat-with-pdf
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/langchain-chat-with-pdf
  original_url: https://huggingface.co/spaces/Chris4K/langchain-chat-with-pdf

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"

# Updated: 2026-01-03 13:45:21
